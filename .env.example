# ========================================
# MediaCMS 环境变量配置示例
# ========================================
# 
# 使用说明:
# 1. 复制此文件为 .env
# 2. 修改下面的值以匹配你的环境
# 3. 确保 .env 文件在 .gitignore 中（不要提交到 git）
#

# Django 核心配置
DJANGO_SECRET_KEY=your-super-secret-key-change-this-in-production-make-it-long-and-random
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com

# 数据库配置 (PostgreSQL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=mediacms
DB_USER=mediacms
DB_PASSWORD=your-secure-database-password
DB_HOST=127.0.0.1
DB_PORT=5432

# Redis 配置
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# Celery 配置
CELERY_BROKER_URL=redis://127.0.0.1:6379/1
CELERY_RESULT_BACKEND=redis://127.0.0.1:6379/1

# 门户配置
PORTAL_NAME=MediaCMS
PORTAL_DESCRIPTION=A modern video and media CMS
TIME_ZONE=Asia/Shanghai

# 文件路径配置
STATIC_ROOT=/path/to/your/static/files
MEDIA_ROOT=/path/to/your/media/files
TEMP_DIRECTORY=/tmp

# FFmpeg 配置 (如果不在 PATH 中，请指定完整路径)
FFMPEG_COMMAND=ffmpeg
FFPROBE_COMMAND=ffprobe
MP4HLS_COMMAND=mp4hls

# 邮件配置 (可选)
# 开发环境可以使用 console backend 在控制台显示邮件内容
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# 生产环境使用 SMTP
# EMAIL_BACKEND=djcelery_email.backends.CeleryEmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-email-password
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
ADMIN_EMAIL_LIST=admin@yourdomain.com

# 安全配置 (生产环境建议设置为 True)
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False
SECURE_SSL_REDIRECT=False
